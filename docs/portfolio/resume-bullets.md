# Résumé / profile bullets (copy-paste)

Drop-in bullets and a short bio. Replace **[bracketed]** specifics. All metrics
trace to merged, tested work in this repo — keep them honest.

## One-line headline
> Quantum & scientific-software engineer — rigorous, reproducible, verification-first.

## ~120-word bio
> **[Name]** builds verification-first quantum and scientific-computing software.
> Recent open-source work (the GaugeGap Foundry) spans certified interval-arithmetic
> numerics, machine-checkable proof export to Lean/Coq/Isabelle, a quantum
> signal-extraction toolkit (QPE, QCELS, classical shadows), and two
> "honest-by-construction" DSLs — ~490 passing tests, byte-reproducible artifacts,
> and a strict claim-boundary audit at zero high findings. [Name] cares about
> results that are reproducible and scope-bounded *before* they are claimed — the
> discipline that matters in research, safety-critical, and AI-evaluation work.
> Available for **[contract / full-time]** in quantum software, scientific
> computing, ML/eval tooling, or formal methods. **[email] · [links]**

## Résumé bullets
- Cut a certified-numerics kernel's runtime **~14.5× (155 s → 10.7 s) with
  bit-identical output**, verified by recorded values + 490 passing tests.
- Exported a certified result to **Lean 4 / Coq / Isabelle** as discharged proofs
  with a single explicit trust input; authored the precise claim-scope docs.
- Built a quantum **signal-extraction** toolkit (Hadamard test, `g(t)`,
  Prony/ESPRIT, QCELS, classical shadows, amplitude estimation) with an exact mode
  for deterministic CI and a dephasing/shot **noise study**.
- Designed two interpreters (**Spectra**, **Verdict**) that make claims fail unless
  backed by a certificate / logged eval — integrity as a language feature.
- Stood up an **optional CUDA-Q GPU backend** (capability-gated, CPU fallback,
  parity-tested) and a multi-workflow GitHub Actions CI with hashed, reproducible
  build artifacts.

## Where to apply (map repo → role)
- **Quantum software** (IBM, quantum startups): `curverank_qpe.py`, `ibm_adapter.py`, CUDA-Q backend.
- **Scientific computing / HPC**: the ~14× certified kernel, interval arithmetic, GPU.
- **ML / LLM-eval & guardrails**: the **Verdict** eval-first DSL.
- **Formal methods**: the Lean/Coq/Isabelle export.
- **Marketplaces**: Toptal, Braintrust, Upwork (lead with the case studies).

> Keep it honest in interviews: the RH/quantum work is a *capability showcase and
> a certified negative result* — not a proof of any Millennium Prize problem.
