# Prospective ledger

This branch is the append-only evidence ledger for Lottery Forge protocol `lottery-forge-0002-prospective-v1`.

Prediction and score artifacts are written by the `Lottery Forge Prospective` workflow only after the protocol implementation is merged to `main`. Existing target-date prediction artifacts must never be replaced with different content.
